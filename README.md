# Double-Slit Experiment Visual Example

This repository contains a single-file interactive visualization of the double-slit experiment.

## Run locally

Open `index.html` in a browser.

For a local server:

```bash
python3 -m http.server 8000
```

Then browse to <http://localhost:8000>.
